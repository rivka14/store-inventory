# Server Configuration
PORT=3001
NODE_ENV=development

# MongoDB Configuration
# Choose ONE of the following:

# Option 1: MongoDB Atlas (Recommended for Deployment)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>.mongodb.net/store-inventory?retryWrites=true&w=majority

# Option 2: Local MongoDB (Development Only)
# MONGODB_URI=mongodb://127.0.0.1:27017/store-inventory
